import { useCallback, useEffect, useState } from 'react'
import { FiEye, FiTrash2, FiX } from 'react-icons/fi'
import { apiRequest } from '../../services/api'

const formatDate = (value) => new Date(value).toLocaleDateString('en-IN', { dateStyle: 'medium' })

export default function AdminEnquiriesPage() {
  const [items, setItems] = useState([]); const [selected, setSelected] = useState(null); const [state, setState] = useState({ loading: true, error: '', notice: '' })
  const load = useCallback(() => apiRequest('/api/admin/enquiries').then(({ data }) => { setItems(data); setState({ loading: false, error: '', notice: '' }) }).catch((error) => setState({ loading: false, error: error.message, notice: '' })), [])
  useEffect(() => { load() }, [load])
  const status = async (item, nextStatus) => { try { const { data } = await apiRequest(`/api/admin/enquiries/${item.id}/status`, { method: 'PATCH', body: JSON.stringify({ status: nextStatus }) }); setItems(items.map((row) => row.id === data.id ? data : row)); setSelected(selected?.id === data.id ? data : selected); setState({ loading: false, error: '', notice: 'Enquiry status updated.' }) } catch (error) { setState({ loading: false, error: error.message, notice: '' }) } }
  const remove = async (item) => { if (!window.confirm(`Delete enquiry from ${item.name}? This cannot be undone.`)) return; try { await apiRequest(`/api/admin/enquiries/${item.id}`, { method: 'DELETE' }); setItems(items.filter((row) => row.id !== item.id)); setSelected(null); setState({ loading: false, error: '', notice: 'Enquiry deleted.' }) } catch (error) { setState({ loading: false, error: error.message, notice: '' }) } }
  return <><header className="admin-header"><div><span className="eyebrow">Management</span><h1>Enquiries</h1></div><button type="button" onClick={load}>Refresh</button></header>{state.error && <p className="admin-notice error">{state.error}</p>}{state.notice && <p className="admin-notice">{state.notice}</p>}{state.loading ? <div className="admin-screen-state">Loading enquiries…</div> : !items.length ? <p className="empty-state">No enquiries found.</p> : <div className="admin-table-wrap"><table><thead><tr><th>Name</th><th>Phone</th><th>Email</th><th>Subject</th><th>Status</th><th>Created</th><th>Actions</th></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td>{item.name}</td><td><a href={`tel:${item.phone}`}>{item.phone}</a></td><td>{item.email || '—'}</td><td>{item.subject}</td><td><b className={`status ${item.status.toLowerCase()}`}>{item.status}</b></td><td>{formatDate(item.createdAt)}</td><td><div className="table-actions"><button type="button" onClick={() => setSelected(item)} aria-label={`View enquiry from ${item.name}`}><FiEye /></button><select aria-label={`Change status for enquiry from ${item.name}`} value={item.status} onChange={(event) => status(item, event.target.value)}><option>NEW</option><option>READ</option><option>REPLIED</option><option>CLOSED</option></select><button className="danger" type="button" onClick={() => remove(item)} aria-label={`Delete enquiry from ${item.name}`}><FiTrash2 /></button></div></td></tr>)}</tbody></table></div>}{selected && <div className="admin-modal" role="dialog" aria-modal="true" aria-label="Enquiry details"><article><button className="modal-close" type="button" onClick={() => setSelected(null)} aria-label="Close enquiry details"><FiX /></button><h2>Enquiry details</h2><dl><dt>Name</dt><dd>{selected.name}</dd><dt>Phone</dt><dd><a href={`tel:${selected.phone}`}>{selected.phone}</a></dd><dt>Email</dt><dd>{selected.email || 'Not provided'}</dd><dt>Subject</dt><dd>{selected.subject}</dd><dt>Status</dt><dd>{selected.status}</dd><dt>Created</dt><dd>{formatDate(selected.createdAt)}</dd><dt>Message</dt><dd>{selected.message}</dd></dl></article></div>}</>
}
